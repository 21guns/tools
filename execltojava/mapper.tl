package com.${package_name}.${module_name}.service.repository.mapper;

import com.${package_name}.common.base.BaseMapper;

import com.${package_name}.${module_name}.service.entity.${entity_name};

public interface ${class_name}Mapper extends BaseMapper<${entity_name}, ${entity_name}> {

}